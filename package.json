{
  "name": "slipher",
  "module": "index.ts",
  "version": "0.5.3",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "bun run build.ts && bun build ./example/movement_and_animation.ts  --target=bun --minify --compile --outfile ./out/main",
    "winfix": "editbin /subsystem:windows ./out/main.exe"
  },
  "type": "module"
}