{
  "name": "wave",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "make_out_dir" : "mkdir out && cd out && mkdir lib && mkdir assets",
    "init_build" : "bun run make_out_dir && cp ./lib/* ./out/lib && cp -r ./assets ./out",
    "build" : "rm -r out && bun run init_build && bun build example/movement_and_animation.ts --compile --minify --outfile ./out/main"
  }
}