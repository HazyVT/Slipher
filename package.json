{
  "name": "slipher",
  "module": "index.ts",
  "version": "0.5.0",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "bun run src/build.ts && bun build main.ts --minify --compile --outfile ./out/main",
    "winfix": "editbin /subsystem:windows ./out/main.exe"
  },
  "type": "module"
}